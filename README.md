# Shrektan's Blog

A Hugo-only static blog.

## Local Development

```bash
hugo server -D
```

## Production Build

```bash
hugo
```

`publish` directory: `public/`

The site can be deployed to any static hosting provider (for example Netlify and Cloudflare Pages) with the same Hugo build command.
